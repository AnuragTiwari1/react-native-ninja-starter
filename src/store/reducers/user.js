const initialState = {
  username: '',
  token: null,
};
function userReducer(state = initialState, { type, payload }) {
  switch (type) {
    default:
      return state;
  }
}

export default userReducer;
